# Works images

Place portfolio images in this folder, then commit and push them to `main`.
Supported formats: PNG, JPG/JPEG, WebP, GIF, and AVIF.

GitHub Actions regenerates `works-manifest.js` automatically. Files are shown in natural filename order, so names such as `01-title.png`, `02-title.png`, ... are recommended.
